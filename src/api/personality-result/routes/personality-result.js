'use strict';

/**
 * personality-result router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::personality-result.personality-result');
